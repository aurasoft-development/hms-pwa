export const RupeeIcon = ({ className = 'w-7 h-7', style = {} }) => {
  return (
    <span className={`flex items-center justify-center font-bold ${className}`} style={style}>
      ₹
    </span>
  );
};

